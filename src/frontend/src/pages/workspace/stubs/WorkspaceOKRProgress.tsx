import { Activity } from "lucide-react";
import StubPage from "../../../components/StubPage";

export default function WorkspaceOKRProgress() {
  return (
    <StubPage
      title="OKR Progress"
      description="Update your OKR realization status and add progress notes for each key result."
      icon={Activity}
    />
  );
}
