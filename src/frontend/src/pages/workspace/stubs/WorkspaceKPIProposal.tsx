import { ClipboardList } from "lucide-react";
import StubPage from "../../../components/StubPage";

export default function WorkspaceKPIProposal() {
  return (
    <StubPage
      title="KPI Proposal"
      description="Draft and submit your KPI proposals for the current performance year."
      icon={ClipboardList}
    />
  );
}
